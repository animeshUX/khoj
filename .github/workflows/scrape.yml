name: Scrape Craigslist

on:
  schedule:
    # 13:00 UTC = 9am EDT / 8am EST. Adjust if you want a different time.
    - cron: "0 13 * * *"
  workflow_dispatch:    # lets you trigger from the Actions tab on demand

permissions:
  contents: write       # so we can commit docs/ back to main

concurrency:
  group: scrape
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Audit deps for known CVEs
        # Fail the run if a pinned dep has a known vulnerability. Dependabot
        # also opens PRs for these, but pip-audit catches anything that landed
        # between the weekly Dependabot scan and now.
        run: |
          pip install pip-audit
          pip-audit --strict --requirement requirements.txt

      - name: Sanity-check Craigslist access
        run: python scraper.py --sanity-check

      - name: Run scraper
        env:
          KHOJ_SUBMISSIONS_URL: ${{ secrets.KHOJ_SUBMISSIONS_URL }}
        run: python scraper.py --pages-mode --max-listings 100

      - name: Commit updated report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Daily scrape: $(date -u +%Y-%m-%d)"
            git push
          fi
